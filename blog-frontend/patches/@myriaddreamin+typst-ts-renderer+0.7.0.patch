diff --git a/node_modules/@myriaddreamin/typst-ts-renderer/pkg/wasm-pack-shim.mjs b/node_modules/@myriaddreamin/typst-ts-renderer/pkg/wasm-pack-shim.mjs
index 188e72a..d4d1777 100644
--- a/node_modules/@myriaddreamin/typst-ts-renderer/pkg/wasm-pack-shim.mjs
+++ b/node_modules/@myriaddreamin/typst-ts-renderer/pkg/wasm-pack-shim.mjs
@@ -1,4 +1,3 @@
-
 import { setImportWasmModule } from './typst_ts_renderer.mjs';
 import _default from './typst_ts_renderer.mjs';
 export * from './typst_ts_renderer.mjs';
@@ -12,7 +11,26 @@ let nodeJsImportWasmModule = async function(wasm_name, url) {
   return await readFileSync(wasmPath).buffer;
 };
 
-// nodejs
+let browserImportWasmModule = async function(wasm_name, url) {
+  try {
+    const wasmUrl = new URL(wasm_name, url);
+    const response = await fetch(wasmUrl.href);
+    if (!response.ok) {
+      throw new Error(`Failed to fetch wasm: ${response.status}`);
+    }
+    return await response.arrayBuffer();
+  } catch (e) {
+    console.warn('Failed to fetch wasm via import.meta.url, trying direct import...', e);
+    try {
+      const wasmModule = await import('./typst_ts_renderer_bg.wasm?url');
+      const response = await fetch(wasmModule.default);
+      return await response.arrayBuffer();
+    } catch (e2) {
+      throw new Error(`Failed to load wasm: ${e.message}, ${e2.message}`);
+    }
+  }
+};
+
 const isNode =
   typeof process !== "undefined" &&
   process.versions != null &&
@@ -20,5 +38,6 @@ const isNode =
 
 if (isNode) {
   setImportWasmModule(nodeJsImportWasmModule);
+} else {
+  setImportWasmModule(browserImportWasmModule);
 }
\ No newline at end of file
-
