diff --git a/node_modules/@myriaddreamin/typst-ts-web-compiler/pkg/wasm-pack-shim.mjs b/node_modules/@myriaddreamin/typst-ts-web-compiler/pkg/wasm-pack-shim.mjs
index e101250..c8cbf84 100644
--- a/node_modules/@myriaddreamin/typst-ts-web-compiler/pkg/wasm-pack-shim.mjs
+++ b/node_modules/@myriaddreamin/typst-ts-web-compiler/pkg/wasm-pack-shim.mjs
@@ -1,4 +1,3 @@
-
 import { setImportWasmModule } from './typst_ts_web_compiler.mjs';
 import _default from './typst_ts_web_compiler.mjs';
 export * from './typst_ts_web_compiler.mjs';
@@ -12,7 +11,15 @@ let nodeJsImportWasmModule = async function(wasm_name, url) {
   return await readFileSync(wasmPath).buffer;
 };
 
-// nodejs
+let browserImportWasmModule = async function(wasm_name, url) {
+  const wasmUrl = new URL(wasm_name, url);
+  const response = await fetch(wasmUrl.href);
+  if (!response.ok) {
+    throw new Error(`Failed to fetch wasm: ${response.status}`);
+  }
+  return await response.arrayBuffer();
+};
+
 const isNode =
   typeof process !== "undefined" &&
   process.versions != null &&
@@ -20,5 +27,6 @@ const isNode =
 
 if (isNode) {
   setImportWasmModule(nodeJsImportWasmModule);
+} else {
+  setImportWasmModule(browserImportWasmModule);
 }
\ No newline at end of file
-
